import 'package:flutter/widgets.dart';

abstract final class AppRadius {
  static const BorderRadius chip = BorderRadius.all(Radius.circular(8));
  static const BorderRadius card = BorderRadius.all(Radius.circular(8));
  static const BorderRadius bottomNav = BorderRadius.all(Radius.circular(8));
  static const BorderRadius modal = BorderRadius.all(Radius.circular(8));
  static const BorderRadius input = BorderRadius.all(Radius.circular(8));
  static const BorderRadius button = BorderRadius.all(Radius.circular(8));
  static const BorderRadius avatar = BorderRadius.all(Radius.circular(8));
}
